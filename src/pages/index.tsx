import type { NextPage } from 'next'

const Home: NextPage = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'sans-serif',
      }}
    >
      <h1>E-mail Serverless API</h1>
    </div>
  )
}

export default Home
